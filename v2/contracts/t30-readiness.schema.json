{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "radar-v2-t30-readiness",
  "title": "Radar V2 XI-event and T-30 readiness result",
  "type": "object",
  "required": ["status", "ready", "analysis_complete", "minutes_to_kickoff", "deadline_minutes", "missing", "next_actions"],
  "properties": {
    "status": {
      "enum": [
        "PREPARING",
        "XI_TRIGGERED_ANALYSIS",
        "AWAITING_FINAL_PRICE_CHECK",
        "FINAL_PRICE_WINDOW",
        "T30_READY",
        "T30_DEADLINE_MISSED"
      ]
    },
    "ready": { "type": "boolean" },
    "analysis_complete": { "type": "boolean" },
    "minutes_to_kickoff": { "type": ["number", "null"] },
    "deadline_minutes": { "const": 30 },
    "missing": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["code"],
        "properties": {
          "code": { "type": "string", "minLength": 1 },
          "detail": { "type": ["string", "null"] }
        },
        "additionalProperties": false
      }
    },
    "next_actions": {
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    }
  },
  "allOf": [
    {
      "if": { "properties": { "ready": { "const": true } } },
      "then": {
        "properties": {
          "status": { "const": "T30_READY" },
          "analysis_complete": { "const": true },
          "missing": { "maxItems": 0 }
        }
      }
    }
  ],
  "additionalProperties": false
}
