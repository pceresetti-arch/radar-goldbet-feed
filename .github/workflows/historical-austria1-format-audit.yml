name: Historical Austria 1 Format Audit

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/historical-austria1-format-audit.yml'
      - 'tools/historical_audit_austria1_format.py'

permissions:
  contents: write

concurrency:
  group: historical-austria1-format-audit
  cancel-in-progress: false

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Audit Austrian Bundesliga format
        run: python tools/historical_audit_austria1_format.py
      - name: Preserve evidence
        run: |
          rm -rf /tmp/austria1-history
          cp -R feed/historical/austria1 /tmp/austria1-history
      - name: Commit format evidence
        run: |
          git config user.name "radar-backtest-bot"
          git config user.email "actions@users.noreply.github.com"
          for attempt in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            rm -rf feed/historical/austria1
            mkdir -p feed/historical
            cp -R /tmp/austria1-history feed/historical/austria1
            git add feed/historical/austria1
            if git diff --cached --quiet; then exit 0; fi
            git commit -m "Add Austria 1 historical format audit"
            if git push; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
