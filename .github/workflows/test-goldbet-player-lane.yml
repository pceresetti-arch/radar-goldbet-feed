name: Test GoldBet player lane [LEGACY MANUAL]

# Retired from Radar V2 operational flow.
# Player discovery and exact-price certification belong to BetFlag V7.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "GoldBet player lane test is legacy/manual only."
          echo "Use BetFlag V7 /live/player-props and /live/player-price."
