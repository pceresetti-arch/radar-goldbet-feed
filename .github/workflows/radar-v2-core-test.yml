name: Radar V2 core tests

on:
  push:
    branches: [radar-v2-core]
    paths:
      - 'v2/**'
      - '.github/workflows/radar-v2-core-test.yml'
  pull_request:
    paths:
      - 'v2/**'
      - '.github/workflows/radar-v2-core-test.yml'

permissions:
  contents: read

jobs:
  core:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run V2 data-gate tests
        run: node v2/test/data-gate.test.mjs
      - name: Run V2 quote-source policy tests
        run: node v2/test/quote-source-policy.test.mjs
      - name: Run V2 standard odds series tests
        run: node v2/test/standard-odds-series.test.mjs
      - name: Run V2 quote-timeline tests
        run: node v2/test/quote-timeline.test.mjs
