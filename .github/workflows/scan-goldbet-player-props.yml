name: Scan GoldBet player props [LEGACY MANUAL]

# Retired from Radar V2 operational flow.
# Player discovery is authoritative through BetFlag V7 /live/player-props.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "GoldBet player scan is legacy/manual only."
          echo "Use BetFlag V7 /live/player-props for player discovery."
