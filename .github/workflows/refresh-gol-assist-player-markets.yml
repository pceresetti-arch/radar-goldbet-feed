name: Refresh Gol Assist player markets [LEGACY MANUAL]

# Radar V2 player lane is BetFlag V7 live:
# /live/player-props for discovery and /live/player-price for exact certification.
# This legacy cache collector is intentionally manual-only to avoid duplicate
# upstream polling, GitHub write collisions and repeated failure emails.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy collector retired
        run: |
          echo "Legacy Gol/Assist collector retired from automatic execution."
          echo "Use BetFlag V7 /live/player-props for live discovery."
          echo "Use BetFlag V7 /live/player-price only for exact candidate certification."
