name: Patch Gol Assist markets [LEGACY MANUAL]

# The legacy cache collector has been retired in Radar V2. This patcher must
# not modify or re-enable it automatically.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy Gol/Assist collector patcher retired."
          echo "BetFlag V7 live discovery owns player-market coverage."
