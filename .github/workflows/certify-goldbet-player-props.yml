name: Certify GoldBet player props [LEGACY MANUAL]

# Retired from Radar V2 operational flow.
# Player exact-price proof is authoritative through BetFlag V7 /live/player-price.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "GoldBet/OddsPapi player certification is legacy/manual only."
          echo "Use BetFlag V7 /live/player-price for exact price certification."
