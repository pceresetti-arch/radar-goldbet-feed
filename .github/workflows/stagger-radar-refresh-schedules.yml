name: Stagger Radar refresh schedules

# Maintenance helper only. Player-prop schedules were retired in Radar V2;
# this workflow must never re-introduce them.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Radar V2 schedule policy
        run: |
          echo "Player collectors are BetFlag V7 live and have no legacy cron."
          echo "Schedule staggering helper retired to avoid reintroducing old player crons."
