# Radar Analysis Archive

Archivio persistente append-only delle analisi Radar.

Percorso: `data/analysis/YYYY-MM-DD/<match_slug>/<timestamp>_<phase>.json`

Ogni snapshot deve rispettare `docs/RADAR_ANALYSIS_ARCHIVE_CONTRACT.md` e mantenere collegamenti alle eventuali giocate in `data/ledger/YYYY-MM-DD.json`.
